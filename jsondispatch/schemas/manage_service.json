{
    "properties": {
        "operation": {
            "type": "string"
        },
        "service": {
            "type": "string",
            "enum": ["syslens"]
        },
        "task": {
            "type": "string",
            "enum": ["start", "stop", "status"]
        },
        "host_nodes": {
            "type": "array",
            "items": {
                "type": "string"
            }
        }
    },
    "required": [
        "operation",
        "service",
        "task",
        "host_nodes"
    ],
    "additionalProperties": false,
    "type": "object"
}
